export enum ToggleValue {
  ON = 'on',
  OFF = 'off',
}

export enum Orientation {
  LANDSCAPE = 'landscape',
  PORTRAIT = 'portrait',
}

export enum LinePlacement {
  TOP = 'top',
  MIDDLE = 'middle',
  BOTTOM = 'bottom',
}
